# 헬스장 이탈/불만 예측 파이프라인 의존성
# (검증된 버전 기준. 다른 환경이면 아래 최소버전 이상 권장)

# --- 추론(predict.py)에 필수 ---
xgboost>=2.0
shap>=0.44
joblib>=1.3
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3      # 번들 언피클/전처리에 필요

# --- 백엔드 API(churn.py, FastAPI)에 추가 ---
fastapi>=0.110
uvicorn[standard]>=0.29
psycopg[binary]>=3.1   # PostgreSQL(Supabase) 드라이버
python-dotenv>=1.0     # .env 로드

# 검증 환경: python 3.13 / xgboost 3.3.0 / shap 0.52.0 / scikit-learn 1.9.0
#            pandas 3.0.3 / numpy 2.4.6

